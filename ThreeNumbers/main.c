#include <stdio.h>
#include <stdlib.h>
float readn(char* prompt) {
    char c[64];
    float* val = NULL;

    while (!val) {
        fgets(word, 63, stdin);
        c[63] = 0;
    }


}

int main() {
    float x = readn();

    return 0;
}
